import Header from '../../Components/Header';
import './Listeners.scss';

const Listeners = () => {
    return (
        <div className="listeners-page">
            <Header />
            <div className="listeners-page__content"></div>
        </div>
    );
};

export default Listeners;
