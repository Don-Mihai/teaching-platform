import Header from '../../components/Header';
import './Profile.scss';

const Profile = () => {
    return (
        <div className="profile-page">
            <Header />
            <div className="profile-page__content"></div>
        </div>
    );
};

export default Profile;
